export class Label {
    constructor(
        public text: string,
        public icon: string, // there should be some default list to chose from
        public color: string // hex
    ) { }
}
